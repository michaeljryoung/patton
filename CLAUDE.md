# Patton

Always-passthrough terminal with a floating compose panel for drafting multi-line input.

## Tech Stack
- **Electron** (v40) with Vite + TypeScript via Electron Forge
- **xterm.js** v6 (`@xterm/xterm`) — terminal rendering (WebGL addon)
- **node-pty** — PTY spawning in main process
- **electron-store** — persistent settings + history

## Architecture

```
src/
├── main/           # Electron main process (PTY, IPC, menus, settings store)
├── preload/        # contextBridge API (type-safe IPC bridge)
├── renderer/       # UI components, services, styles
│   ├── components/ # Tab, Terminal, Compose Panel, Settings, Command Palette, etc.
│   ├── services/   # Tab manager, history, keybindings
│   └── styles/     # CSS with design tokens + auto dark/light mode
├── shared/         # IPC channel constants + shared TypeScript types
└── resources/      # Shell integration scripts (zsh, bash)
```

## Commands
- `npm start` — dev mode with hot reload
- `npm run package` — build distributable
- `npm run make` — create DMG installer
- `npm run lint` — ESLint
- `npm run release [-- patch|minor|major]` — manual release (CI handles this automatically)

## Verifiers

```bash
npm run lint && npm run package
```

## Key Conventions
- All colors use CSS variables (`:root` + `@media prefers-color-scheme`)
- All interactive elements have ARIA attributes
- Resize observers are debounced (100ms)
- PTY data is write-coalesced (4ms buffer)
- electron-store needs CJS/ESM interop: `(mod.default || mod)`
- Entry files must match package.json `main` field naming (main.ts → main.js, preload.ts → preload.js)

---

## Status
Feature-complete, distributed at **v1.9.0**. S34's URGENT shortcuts-registry item is DONE — one registry now feeds menu, palette, Settings grid, context menu and onboarding. Homebrew auto-update **confirmed green** (S33 deploy key, first clean cask step since S28). Backlog: OSC 8 hyperlinks, dev-dep CVE pass, double-click-link validation, S20 punch-list. Open: the v1.9.0 UI features are build-verified but not yet driven through a live UI.

## Mission Synthesis
Patton is a macOS Electron terminal with always-passthrough xterm.js input and a floating compose panel for multi-line drafting, built to close stock-terminal gaps Claude Code exposes (colour-query responses, COLORFGBG, contrast safety net, attention indicators). Feature-complete and distributed at v1.9.0 (DMG + Homebrew tap, self-signed, CI auto-versioned on push to main). Its defining saga — garbled-glyph corruption in xterm's WebGL glyph atlas — closed across S24–S30 with a structural fix: a GPU-free DOM renderer, default since S30, WebGL a one-click opt-in. S36 closed the arc S34 opened: five hand-maintained copies of the same shortcut facts (menu, palette, Settings grid, pane context menu, onboarding) collapsed into one registry in `src/shared/shortcuts.ts`, glyphs derived from Electron accelerators — the Settings grid went 14→26 rows and can no longer drift. The same release added user-set tab colour flags and made the close-confirmation dialog keyboard-drivable (←/→ plus a focus ring it had always lacked), and fixed a build hook that had been silently killing the running app: the install step's `rm -rf` on `/Applications/Patton.app` unlinked the bundle under a live process. v1.9.0 was the first release with a green Homebrew cask step since S28, confirming S33's SSH deploy key. Next arc: validate the v1.9.0 UI in real use (build-verified only), then OSC 8 hyperlinks, the dev-dep CVE pass, and the S20 punch-list. Constraints: self-signed dist, ⌘Q+relaunch after packaging, local builds self-report 1.0.0. Lessons: hand-maintained duplicates of code-defined facts always drift — generate them; test the acceptance criterion rather than assuming it; a security pin is point-in-time and ages out silently.

## Session Log
**2026-08-01 (session 36)** — closed the S34 URGENT shortcuts-registry item; added tab colour flags + a keyboard-drivable confirm dialog; fixed a build hook that was killing the running app; shipped **v1.9.0** with Homebrew auto-update green for the first time since S28
- **Single shortcuts registry** (`da88951`, new `src/shared/shortcuts.ts`) — the mandated S34 structural fix. Found **five** hand-maintained copies of the same facts, not the three S34 knew about: `menu.ts`, the palette, the Settings grid, plus **`pane.ts`'s context menu** and **`onboarding.ts`**. All now read one registry (id + label + Electron accelerator), with `acceleratorToDisplay()` deriving the ⌘⇧ glyphs so a binding is written once. Settings grid **14 → 26 rows**: every shortcut S34 named, plus the two it flagged unaudited (⌘⇧K, and Quick Terminal showing the **live** `globalHotkey`). Acceptance *tested, not assumed* — a throwaway entry surfaced in Settings + palette with zero other edits; every generated glyph diffed against the old strings (all identical, palette membership unchanged), so it's a refactor plus the missing rows.
- **Tab colour flags + keyboard-drivable confirm dialog** (`0018383`, user-requested). Right-click a tab → Green/Yellow/Red/Clear: independent of the automatic dot, survives activating the tab (the actual gap), shows on the active tab, re-pick toggles off, persists via `SessionTabState.flag` on the existing 30s autosave, rendered static so it stays distinguishable from the pulsing awaiting-input dot. Dialog: ←/→ move between Cancel and OK (→ then Enter closes) with Cancel still default. Root cause of it feeling mouse-only — the footer buttons had **no `:focus` style**, so arrows moved an invisible selection.
- **Fixed a build hook that was killing the running app** (`9a5f13d`). `forge.config.ts`'s install step ran `rm -rf /Applications/Patton.app && cp -R`, and the post-commit hook fires it on *every* commit; unlinking the bundle kills a live Patton the next time it lazily loads from it — quietly, **no crash dump**. It took down the user's 8-day-old session mid-work. Now `ditto` (overwrite in place, inodes survive).
- **Security: `fast-uri` ^3.1.2 → ^3.1.4** (`4af8d01`). Two *new* high advisories extended the vulnerable range to 3.1.3, outgrowing the S29 pin, so `npm run package`'s audit gate failed before the build ran. Prod tree back to 0 high advisories.
- **Shipped v1.9.0** — all CI steps green first try **including `Update Homebrew cask`**, red on every release since S28; confirms the S33 deploy key and ends the manual tap bump. Checksum independently verified by streaming the published DMG through `shasum`. Local /Applications reinstalled from the canonical DMG (version string 1.9.0, signature valid); features confirmed in the shipped asar by grepping *string literals* (identifiers minify), with the old grid's `Jump between prompts` literal **absent** — positive proof the generated grid replaced it.
- **Verification limits, stated plainly.** The UI features are lint/type/build/asar-verified, **not driven through a live UI**. Three routes failed: the signed build refuses the Node inspector (no `get-task-allow` — S33's CDP trick needs the *dev* app), dev mode raced Vite after `npm install`, and an isolated-profile launch popped a **keychain prompt onto the user's screen**, at which point GUI automation was abandoned rather than click a credential dialog.

**2026-07-29 (session 35)** — estate session: ESLint PostToolUse pilot wired into `.claude/settings.local.json`; config-only, no code changes
- A global estate-maintenance session added a project-local PostToolUse hook: on any `.ts`/`.tsx` Edit/Write, `npx eslint --no-warn-ignored <file>` runs and feeds the first 30 lines back to Claude — lint findings surface at edit time instead of waiting for the verifier pass. Loads from the next Patton session.
- Judge after the first real coding session: keep, tune, or delete the `hooks` block (tracked in NEXT_STEPS). The URGENT S34 shortcuts-registry item remains first up.

**2026-07-07 (session 34)** — notes panel validated live; found + logged the Settings shortcuts-list drift as URGENT with a mandated structural fix; docs-only (2 commits, no release)
- **Notes panel confirmed working in real use** — user toggles it with ⌃⌘N in the packaged v1.8.0 app; walked through verified behavior (400ms-debounced autosave + flush-on-close, Escape closes and refocuses the terminal, drag-resize clamped 200px–80% with width remembered, one global document, encrypted store, ~1 MB cap, lazy-loaded on first open).
- **User spotted ⌃⌘N missing from Settings → Keyboard Shortcuts.** Root cause: the grid is a hand-maintained HTML block (`settings-panel.ts:138`) that drifts every time a shortcut lands. Bug-propagation audit vs `menu.ts` accelerators found ~8 missing entries: ⌃⌘N notes, ⌘⇧B broadcast, ⌘S save output, ⌘N new window, ⌘=/⌘− font size, ⌘⇧]/[ tab nav, ⌘1–9, plus unaudited quick-terminal hotkey (`main.ts:79`) and ⌘⇧K.
- **Logged as the URGENT top item in `docs/NEXT_STEPS.md`** (`ace9d1e`), then strengthened per user directive (`a8a7209`): hand-adding rows is ruled out — required fix is a single shortcuts registry in `src/shared/constants.ts` (id + label + Electron accelerator + display keys) read by menu, palette, and a runtime-rendered Settings grid; acceptance = a new shortcut appears in Settings with zero `settings-panel.ts` edits. No code changes by explicit request (user mid-live-session).
- Verified `.git/hooks/post-commit` only rebuilds in background (no pkill) → doc-only commits are safe during live app use.

**2026-07-07 (session 33)** — shipped **v1.8.0**: instant pane-close + Ctrl+Cmd+N notes scratchpad; permanently fixed Homebrew auto-update via an SSH deploy key
- **Fixed "can't close a pane."** Diagnosed by driving the dev app headlessly through the Node inspector (CDP): ⌘W *did* pop the S31 "Close this pane?" dialog, but it's Cancel-defaulted, so the natural ⌘W-then-Enter hit Cancel and the pane never closed — and panes have no ✕/context-menu close either. Fix (`bede3f9`, `tab-manager.ts`): scope the confirmation to whole-**tab** closes (the accidental-⌘W loss S31 actually guards); a split **pane** now closes instantly on ⌘W (scrollback still saved for ⌘⇧T). Verified live: split→⌘W instant; whole-tab close still confirms.
- **Added a notes scratchpad** (`886e74b`, ~194 LOC + new `notes-panel.ts`). ⌃⌘N (menu Shell → Toggle Notes, or command palette) toggles a plain-text panel docked right of the terminal — no PTY/shell/compose. Content autosaves (encrypted, electron-store `notes` key, ~1 MB cap), width in localStorage, focus returns to the terminal on close. Built as a docked sidebar (wrapped `#content` in a `#main` flex row) NOT a split-tree node — leaves the terminal's pane/split/session/PTY code untouched. Verified end-to-end via CDP (toggle, save, survives reload, docking dims).
- **Shipped v1.8.0.** Pushed → CI; Build DMG green first try (no flaky `hdiutil` this time); release + `Patton-1.8.0-arm64.dmg` published. Lint clean, `npm run package` 0 prod vulns (the 2 pre-existing `tsc` notification-sound errors untouched). Tap hand-bumped 1.7.0→1.8.0 with three-way checksum verification.
- **Fixed Homebrew auto-update for good** (`37a8e1e`, `[no-release]`). Root cause: the broad `HOMEBREW_PAT` expired 2026-03-09, failing the cask step every release S28–S32. Replaced with a **write-scoped SSH deploy key** on the tap (least-privilege, no expiry): generated the keypair, added it read-write to `homebrew-patton`, verified read+write end-to-end (real push), stored the private half as the `HOMEBREW_DEPLOY_KEY` secret, rewired `release.yml` to clone/push over SSH, replicated the exact runner logic locally, wiped the key from disk. Guard now skips gracefully if the key is absent (fixes the S28 expired-token-fails-not-skips bug). The deploy key confirms green on the next real release; old `HOMEBREW_PAT` secret now unused.

_S29–S32 (S29 Claude Code fullscreen fallout → DOM-renderer toggle + selection fix, v1.5.0; S30 DOM promoted to default, v1.6.0; S31 recovered a crashed session → confirm-before-close, v1.7.0; S32 verification-only) archived 2026-08-01 S36 trim → `docs/session-archive.md`. S31 kept inline below — it is the origin of the confirm dialog S36 reworked._

**2026-06-30 (session 31)** — recovered a crashed session's uncommitted confirm-before-close feature and shipped it → **v1.7.0**; tap + local copy bumped to match
- **Recovered the crash.** The prior same-day session (`625a6918`) died mid-verification (last action logged: *"re-verify both edits with lint and a type check"*), leaving an uncommitted *confirm-before-closing-a-tab/pane* feature on disk. Reconstructed intent via transcript forensics — the `last-prompt` record held the original ask (*"too easy to accidentally shut a tab with cmd-w… our block only applies if someone shuts the whole [app]"*) — plus file mtimes. No work lost, only the conversation.
- **Committed the feature** (`f935a2b`, 246 LOC / 4 files): new generic `ConfirmDialog` (focus-trapped, ARIA, Cancel-defaulted) + `tab-manager` wiring (✕/middle-click `confirmAndCloseTab`, ⌘W `closeActivePane`; **skips the last-tab case** so it falls through to the app-quit guard — no double prompt; **automatic PTY-exit closes never prompt**) + `settings.css` + a `paste-dialog` focus-trap fix (drop pre-focus of Cancel so `trapFocus` captures the real prior focus). Lint clean; user confirmed it works live (⌘W pops the dialog).
- **Shipped v1.7.0.** Pushed to main → CI. **Attempt 1 failed on a flaky `hdiutil detach … No such file or directory`** in the DMG maker (package + zip were fine) → `gh run rerun --failed` → attempt 2 green. MINOR bump (lines-since-v1.6.0-tag > 500, incl. the S30 doc checkpoint). GitHub release + `Patton-1.7.0-arm64.dmg` published; only the Homebrew-cask step red (expired PAT, as every release since S28).
- **Hand-bumped the tap** `1.6.0→1.7.0` (`homebrew-patton` `72c8d40`); SHA-256 independently verified by streaming the published DMG through `shasum` (`42ad3c08…`). `brew upgrade --cask patton` now serves latest.
- **Updated the local copy.** /Applications/Patton.app is code-current with v1.7.0 — the post-commit hook rebuilds + copies it on every commit, so the confirm-on-close feature is already there (user action: ⌘Q + relaunch). Also installed the canonical DMG via mount → `ditto` over the running bundle (no force-quit, since the session may be inside Patton) → detach, which set the version string to 1.7.0 *momentarily* — but the same hook reverts it to 1.0.0 on the next commit (incl. this checkpoint), so the cosmetic version-string item is moot while the hook runs (see Gotchas).

_S28 (shipped v1.4.0 — pushed S25+S27 fixes; user-confirmed ~2 weeks zero garbled-glyph recurrence + 60k+ defence fires in `main.log` → render saga closed; `HOMEBREW_PAT` confirmed as the expired token) archived 2026-07-07 S34 trim → `docs/session-archive.md`._

_S27 (comprehensive WebGL-atlas defensive pass — `backgroundThrottling:false` + visibility/focus flush + renderer-log IPC bridge; late-session iTerm2 repro reoriented it to a possible system-level cause; shipped v1.4.0, validated S28) archived 2026-07-07 S33 trim → `docs/session-archive.md`._

_S24–S26 (render-corruption defensive hardening → atlas-eviction diagnosis + Phase 1 → pre-S25-binary recurrence) archived 2026-05-31 S28 trim → `docs/session-archive.md`. The saga closed in S28: shipped v1.4.0, validated zero recurrence._

_S21–S23 (file logger + health script + dot rewire + quit-confirm + `/mcp` Enter audit) archived 2026-05-07 S27 trim → `docs/session-archive.md`._

_Verbose S19 + S20 live in `docs/session-archive.md` (moved 2026-04-24). Sessions 18 and earlier predate the log; see `git log --grep=Checkpoint`._

## Decisions
- **2026-08-01 (S36) — Registry holds the facts; consumers keep their layout.** `menu.ts` still owns menu *structure* (which submenu, order, separators) and only takes label + accelerator from the registry. Pushing structure in too would have made the registry a menu-description language serving one consumer badly. Also: the registry lives in its own `src/shared/shortcuts.ts`, not in `constants.ts` as S34 suggested — ~30 entries plus a converter would have swamped a file of small constants, and the directive's binding requirement was *one* registry, not a particular file. Renderer-only bindings (⌘E, ⌘⇧K, ⌃R, ⌘⌥arrows) are marked `rendererOnly` and `menu.ts` **throws** if one becomes a menu item, since a main-process accelerator swallows the keystroke before the renderer sees it.
- **2026-08-01 (S36) — Kept Patton's non-standard modifier order.** `acceleratorToDisplay()` renders ⌃⌘⌥⇧ (⌘⇧D, ⌃⌘N), not Apple's canonical ⌃⌥⇧⌘ (⇧⌘D). Normalising would have silently reshuffled every string users already know, for a cosmetic gain unrelated to the drift bug. Logged as a one-line `MODIFIER_ORDER` change if the native look is ever wanted.
- **2026-08-01 (S36) — Tab flags are user-owned, not another automatic signal.** The existing dot is raised and cleared by shell activity; the reported gap was that reading a tab erased it. So a flag is set/cleared *only* by the user, survives activation, and shows on the active tab (hiding your own marker on the tab you're looking at defeats the point). Rendered **static** while the automatic dot keeps pulsing — pulse means "just happened", static means "I marked this", so the two stay distinguishable when both are on screen. Persistence rides the existing 30s session autosave rather than a new save path, mirroring `renameTab`.
- **2026-08-01 (S36) — Confirm dialog: added arrow-key nav, kept the Cancel bias.** S31 made Enter-on-Cancel the protective default and S33 scoped the dialog to whole-tab closes; neither addressed it being effectively mouse-only. ←/→ + Enter makes confirming two deliberate keystrokes while a reflexive bare Enter still cancels — the guard's intent survives. The real defect was a **missing `:focus` style**: keyboard focus existed but was invisible, so the dialog *read* as mouse-only. Fixed with `:focus`, not `:focus-visible`, because focus here is moved programmatically and by arrow keys, which `:focus-visible` may not match.
- **2026-08-01 (S36) — `ditto` over the bundle, not `rm -rf` + `cp -R`.** Chosen over the alternative (skip the install when Patton is running), which would have let `/Applications` go silently stale exactly while the user works. `ditto` overwrites in place so a live process keeps its open inodes — the same no-force-quit path S31 validated for DMG installs. Trade-off accepted: files deleted in a newer build linger, so a truly clean install now needs an explicit `rm` first. Corrects the S34 conclusion that the hook was "safe during live app use because it has no `pkill`" — true premise, wrong conclusion, and the failure is silent (no crash dump), which is what made it hard to attribute.
- **2026-08-01 (S36) — Abandoned live UI verification rather than click a credential dialog.** Three routes were tried and each has a durable lesson: the *packaged* build is signed without `get-task-allow` so it refuses the Node inspector (S33's CDP technique needs the **dev** app); dev mode raced Vite's dependency re-optimisation after `npm install`; and launching with an isolated `--user-data-dir` made safeStorage mint a new key, popping a **keychain password prompt onto the user's screen**. Stopped there — automating clicks through a credential prompt on a machine in active use is not a verification step worth taking. Shipped on lint/type/build/asar evidence with the gap stated plainly instead of implied.
- **2026-07-07 (S34) — Shortcuts-list fix must be structural, not additive.** ⌃⌘N was missing from Settings' Keyboard Shortcuts because the grid is a hand-maintained HTML block (`settings-panel.ts:138`) — audit found ~8 shortcuts had drifted out, not one. User directive: hand-adding the rows is ruled out as an end-state; required fix is one shortcuts registry (`src/shared/constants.ts`: id + label + Electron accelerator + display keys) read by `menu.ts`, the command palette, and a runtime-rendered Settings grid. Acceptance: a new shortcut appears in Settings with zero `settings-panel.ts` edits. Fix deliberately deferred — user was mid-session in the live app; docs-only commits only (`ace9d1e`, `a8a7209`).
- **2026-07-07 (S33) — Scoped close-confirmation to whole-tab closes; pane closes are instant.** S31 bundled pane-close into the tab-close guard, but the Cancel-defaulted dialog turned the natural ⌘W-then-Enter into a *cancel* → users literally couldn't close a pane. The S31 ask was about accidental *tab* loss; closing one pane of a split is low-stakes (tab + sibling panes survive, scrollback saved for ⌘⇧T). So ⌘W on a multi-pane tab now closes the focused pane with no prompt (iTerm2's idle-split behaviour); whole-tab and ✕/middle-click still confirm. Partially supersedes the S31 "confirm-on-close covers tab+pane" decision.
- **2026-07-07 (S33) — Notes pane is a docked sidebar, not a split-tree node.** `Pane` is deeply terminal-coupled (PTY, compose, session serialization, PTY routing); making the split tree polymorphic to hold a notes leaf would touch the most critical code in the app for marginal benefit. A right-docked panel (sibling of `#content`, toggled by ⌃⌘N, persisted in electron-store) delivers "notes alongside your work" while leaving pane/split/session/PTY untouched — minimal-diff, KISS. Trade-off accepted: one notes surface per window, fixed dock position (not arbitrary split placement) — fine for a scratchpad. Flagged the placement as a judgment call for the user (built while he was on a call).
- **2026-07-07 (S33) — Homebrew auto-update fixed with an SSH deploy key, not a refreshed PAT.** Refreshing `HOMEBREW_PAT` needs the browser (no CLI PAT minting) and would re-expire. A write-scoped deploy key is CLI-mintable, least-privilege (writes ONLY the tap repo vs the PAT's all-repo scope), and never expires. Cost: a workflow edit (HTTPS+PAT clone → SSH clone) — contained to the already-failing cask step, so zero blast radius on the release itself. Validated the key + the exact runner logic (env-var→file→`GIT_SSH_COMMAND`→clone) end-to-end locally before trusting it. Held the workflow push behind `[no-release]` to avoid a spurious release. Supersedes the S30 "fix the tap by hand each release" decision.
_S28 Decisions (4: shipped-v1.4.0-accepting-red-cask-step, Phase-2-Canvas-DROPPED, font-cache-reset-DROPPED, no-release.yml-hardening) and S30 Decisions (2: DOM-as-default, hand-fixed-tap-not-token) archived 2026-08-01 S36 trim → `docs/session-archive.md`._
_S31 Decisions (4: recovered-crashed-session-rather-than-redoing, confirm-on-close-covers-tab+pane [partially superseded S33], re-ran-CI-on-flaky-DMG-rather-than-hardening, reinstalled-from-canonical-DMG [superseded S36 ditto]) archived 2026-08-01 S36 trim → `docs/session-archive.md`._
_S29 Decisions (4 entries: renderer-choice-not-more-flushing, `fast-uri` via `overrides` not `audit fix --force`, `macOptionClickForcesSelection` real-gap-fix, shipped-as-one-release-after-live-validation) archived 2026-07-07 S33 trim → `docs/session-archive.md`._

_S25–S27 Decisions (14 entries: backgroundThrottling root-cause fix, dual visibilitychange+focus listeners, auto-reload-on-flush-error, IPC log forwarding over console-patching, hasFocus-not-hidden gating, periodic flush 10→2min, single bundled commit; S26 pre-S25-binary-recurrence-not-a-Phase-2-trigger + diagnostic-only checkpoint; S25 periodic clearTextureAtlas, pre-flush auto-snapshot, defer-Phase-2, single 155-LOC commit, S24-watching-brief-superseded) archived 2026-06-08 S30 trim → `docs/session-archive.md`._
_S24 Decisions (7 entries: defensive-hardening-over-repro, unified double-click links, `event.detail===2` gate, VS Code app-bundle resolution, `shell.openPath` fallback, user-triggered Reset Renderer, synchronous theme-swap atlas reload) archived 2026-05-31 S28 trim → `docs/session-archive.md`._
_S20–S23 Decisions (21 entries: Enter audit, dot signal paths, quit-confirm surfaces, file-logger eager-create, safeStorage rewrite, CommandDoneCoordinator, crash circuit breaker, autosave cadence, startup-command timing, shell allowlist consolidation, crashpad config, ZDOTDIR/rcfile shell-integration, kitty Shift+Enter, COLORFGBG + OSC 10/11, minimumContrastRatio 4.5) archived 2026-05-07 S27 trim → `docs/session-archive.md`._

## Gotchas
- **Claude Code v2.1.x "fullscreen rendering" breaks selection AND can re-trigger glyph corruption** (S29, 2026-06-05). Opt-in research preview (alt-screen buffer + mouse capture; the "Jump to bottom" pill is the tell). Mouse capture sends drags to Claude Code not the terminal → "can't highlight text"; the alt-screen path also re-stresses xterm's WebGL atlas. Disable via `/tui default` (full revert) or `CLAUDE_CODE_DISABLE_MOUSE=1` (keep flicker-free). Patton's answer: the renderer toggle + `macOptionClickForcesSelection`. Docs: `code.claude.com/docs/en/fullscreen`.
- **On macOS, xterm v6 ignores Shift for selection during mouse mode** (S29). `shouldForceSelection` = `isMac ? altKey && macOptionClickForcesSelection : shiftKey`. So on Mac you MUST set `macOptionClickForcesSelection: true` (now set in `terminal-view.ts`) and hold **Option** to drag-select while an app (Claude Code fullscreen, vim, tmux, htop) holds the mouse. Without it there is no selection escape hatch at all.
- **DOM renderer is the structural fix for glyph corruption — and as of S30 it's the DEFAULT** (`DEFAULTS.RENDERER='dom'`). It never loads (or disposes) the WebglAddon → xterm falls back to DOM → no GPU glyph atlas → the whole corruption class is structurally impossible. Fresh installs land here; WebGL ("GPU — fast") is a one-click opt-in (palette → "Switch to GPU Renderer", or Settings → Text Renderer). Existing users' saved choice overrides the default. Confirm DOM is active by `[RENDER] *atlas flush` lines being ABSENT in `main.log` (flush paths no-op when `!webglAddon`). Cost: marginally heavier on huge output bursts.
- **`npm run package` can be blocked by a prod-dependency CVE, not a code error** (S29). The script is `npm audit --omit=dev --audit-level=high && electron-forge package` — one high-severity *production* advisory fails the gate before the build runs (S29: `fast-uri`). Diagnose with `npm audit --omit=dev --audit-level=high`; fix transitive vulns with a `package.json` `overrides` pin, not `audit fix --force`. Dev-only vulns don't count against this gate.
- **`backgroundThrottling: false` in `webPreferences` is load-bearing** for the WebGL atlas-corruption defence (S27, 2026-05-07). With Chromium's default (true), the renderer is suspended/throttled when Patton goes background → on resume the WebGL texture state is silently stale or corrupted → user sees garbled glyphs until window-resize forces a reflow. Removing this setting will reintroduce the bug. Side effects: visibilityState reports 'visible' even when occluded (any "is app in background?" check must use `document.hasFocus()` not `document.hidden`); minor extra background CPU. The visibility-flush handler in `terminal-view.ts` is the recovery fallback — both layers exist deliberately.
- **`document.hidden` is unreliable in Patton** (S27). Because `backgroundThrottling: false` pins the Page Visibility API to 'visible' even when occluded, `document.hidden` is approximately always false in production. Use `document.hasFocus()` for "is the user looking at Patton?" gating. Already replaced in `app.ts:33` for the notification path; if you find another `document.hidden` callsite, it likely needs the same fix.
- **Renderer `console.*` calls don't reach `main.log` unless they go through `rlog()`** (S27). `src/main/logger.ts` only tees the main-process console. Any new `[RENDER]` or other diagnostic line you want in the file log MUST use `rlog()` from `src/renderer/services/renderer-logger.ts` — `console.info` alone reaches DevTools only and is invisible to packaged-app launches. This is exactly the gap that made S25's "is the periodic timer firing?" question unanswerable for ~10 days. `rlog()` keeps the DevTools console output too, so it's a strict superset of `console.*` for diagnostic purposes.
- **`[RENDERER]` prefix in `main.log` marks renderer-bridged lines** (S27); everything without it is native main-process. Matters when grepping: `grep '\[RENDERER\] \[RENDER\] resume atlas flush'` finds visibility-triggered flushes, vs `grep '\[RENDER\] snapshot saved'` (no prefix) which is the main-process snapshot writer.

- _Gotcha "xterm WebGL atlas eviction = the garbled-glyphs root cause (saturation- and focus-driven)" archived 2026-08-01 S36 trim → `docs/session-archive.md` — moot now DOM is the default renderer; only reachable by a WebGL opt-in user._
- **`clearTextureAtlas()` is on Terminal, not WebglAddon** (xterm 6 typings — `node_modules/@xterm/xterm/typings/xterm.d.ts`). Public API; cheap; rebuilds atlas lazily on next render. **No addon disposal needed.** S24's `setCustomTheme()` path disposes+reloads the WebglAddon (more expensive, ~1 frame flicker) — keep that pattern only for when colors actually change. For periodic / heuristic flushes, plain `clearTextureAtlas()` is the right tool.
- **`app.getVersion()` in `main.log` returns `1.0.0` even after CI-versioned releases.** The release script tags git but doesn't bump `package.json` (per repo convention — git tags are source of truth). The `Patton main process started (vX.Y.Z, ...)` startup line is therefore meaningless for diagnosing "is the running build current?". Use the asar mtime instead: `ls -lt /Applications/Patton.app/Contents/Resources/app.asar` — that timestamp matches when `npm run package` last ran. The same `1.0.0` shows in `/Applications/Patton.app`'s `CFBundleShortVersionString`: even after installing the *released* DMG (which carries the real version), the **post-commit hook re-`package`s + copies a 1.0.0 build over it on the very next commit** — so a DMG install's correct version string never survives the next commit (S31). The durable fix is to stamp the version into `package.json`/the build.
- **Homebrew tap auto-updates via a write-scoped SSH deploy key** (S33, 2026-07-07 — replaced the expired `HOMEBREW_PAT`). `michaeljryoung/homebrew-patton` holds one cask, `Casks/patton.rb` (local clone `~/Projects/homebrew-patton`). `release.yml`'s `Update Homebrew cask` step clones/pushes it **over SSH** using the `HOMEBREW_DEPLOY_KEY` secret (private half of a read-write deploy key on the tap — least-privilege, no expiry). The old broad `HOMEBREW_PAT` expired 2026-03-09 and *failed* the step every release S28–S32 (each needed a manual bump); it's now unused and deletable. The guard now skips gracefully when the key is absent. **Manual fallback** if the step ever fails: edit `version` + `sha256` (the `url` auto-resolves from `version`), push via gh OAuth — VERIFY the sha first (`curl -sL <dmg-url> | shasum -a 256`; a wrong hash silently breaks every `brew install`; it's also in the release body's `### Checksums` table). To rotate the deploy key: `ssh-keygen` → `gh repo deploy-key add <pub> --repo michaeljryoung/homebrew-patton --allow-write` → `gh secret set HOMEBREW_DEPLOY_KEY --repo michaeljryoung/patton < <priv>`.
- **The CDP trick works on the DEV app only — the packaged build refuses the inspector** (S36). `/Applications/Patton.app` and `out/…` are signed *without* `get-task-allow`, so `--inspect` / `process._debugProcess` never opens a debuggable target (it starts and logs normally, which makes it look like a hang rather than a refusal — check `codesign -d --entitlements -`). Use `npm start`. Two further traps found S36: a `npm install` immediately before `npm start` makes Vite re-optimise deps, and Electron races it → `ERR_CONNECTION_REFUSED` on `localhost:5173` with forge exiting; and launching *any* build with an isolated `--user-data-dir` makes safeStorage mint a new key, which pops a **keychain password prompt on the user's screen**. Never automate through that prompt — kill the instance instead.
- **Drive the running dev app headlessly via the Node inspector to verify UI** (S33). `node -e 'process._debugProcess(<mainPID>)'` opens port 9229 on the Electron MAIN process; a CDP `Runtime.evaluate` there can `require('electron')` and `webContents.executeJavaScript(...)` into the renderer — so you can split/close panes, toggle UI, read DOM state, and `capturePage()` (offscreen) WITHOUT touching the user's screen (safe while they're on a call). Two gotchas: `Runtime.evaluate` needs `includeCommandLineAPI:true` or `require` is undefined; and a long-lived session accumulates stale async rejections that surface as spurious `require is not defined` — restart the app if evals wedge. This is how S33 diagnosed the pane-close bug + verified the notes panel without a visible window.

- _Gotcha "garbled-glyph symptom is not always a Patton bug — reproduces in iTerm2; discriminate via Apple Terminal.app; `atsutil databases -remove` resets CoreText cache" (S27) archived 2026-07-07 S33 trim → `docs/session-archive.md` (render saga closed, DOM is default)._
- **Committing used to kill a running Patton — the post-commit rebuild replaced `/Applications/Patton.app` under it** (S36, 2026-08-01). `forge.config.ts`'s `postPackage` install step ran `rm -rf /Applications/Patton.app && cp -R …`, and `.git/hooks/post-commit` runs `electron-forge package` on **every** commit. Deleting the bundle out from under a live process kills it the next time it lazily loads from that bundle — it dies quietly, with no crash dump (looks like a clean exit, which is why it's easy to misattribute). This corrects the S34 session-log claim that the hook is "safe during live app use" because it has no `pkill`: the absence of a `pkill` was never the whole story. **Fixed in S36** — the step now uses `ditto "$appPath" "$dest"`, which overwrites in place and leaves the running process's open inodes alive (the same no-force-quit path S31 validated for DMG installs). Trade-off: files deleted in a newer build linger, so `rm -rf /Applications/Patton.app` by hand for a truly clean install. If a running Patton ever dies for no clear reason right after a commit, check this step first.
- _Gotchas "`setCustomTheme()` runs before `mount()` — guard `if (this.webglAddon)`" and "CI's Build DMG can fail on a flaky `hdiutil detach` — just re-run (tag/release steps are gated behind it, so no release publishes)" archived 2026-08-01 S36 trim → `docs/session-archive.md`._
- **macOS does NOT hot-swap a replaced `.app` bundle.** After `npm run package` reinstalls to `/Applications/Patton.app`, the running Patton process is still the OLD binary. ⌘Q + relaunch required to pick up new code. Detection recipe: `ls -lt /Applications/Patton.app/Contents/Resources/app.asar` (asar mtime) vs `ps -o lstart= -p $(pgrep -f Patton.app | head -1)` (process start). If process start < asar mtime, the running build is stale. S26 (2026-04-27 evening) hit this exactly. Second confirmation channel: grep `main.log` for the post-fix log signature.
- **Packaged asar minifies identifier names but preserves literal values.** Verifying a shipped change: don't grep for original variable names (`event.detail !== 2` minifies to `a.detail!==2`). Grep for literal string constants (`'Reset Renderer'`, regex source strings) — those survive minification.
- **Packaged Electron apps launched from Dock/Finder route main-process stderr to `/dev/null`.** Without a file logger, every main-process signal is invisible. See `src/main/logger.ts`. Renderer console doesn't even reach that — see the rlog gotcha above.
- **WebGL glyph atlas is keyed by color and can go stale on theme swap.** xterm's WebGL renderer caches glyphs keyed by `(char, bold, italic, fg_color, bg_color, ...)`. `terminal.options.theme = newTheme` mutates resolution but does NOT invalidate the cache — fix is dispose+reload the WebglAddon on every theme change. Same shape as the `minimumContrastRatio` / `onContextLoss` story.
- **xterm.js v6 registers no kitty/CSI-u handler by default.** Apps sending `\x1b[>1u` (push progressive-enhancement flags) get silently ignored — terminal keeps sending `\r` for Enter, not `\x1b[13u`. Patton's current consumers (Claude Code, fish, neovim) work fine without it.
- **OSC 133 `'prompt'` is not "awaiting input".** Fires on every prompt redraw — shell startup, session restore, SIGWINCH, `clear`, async-prompt refreshes (Starship, Powerlevel10k). Use `pane.ts fireCommandDone()` for the meaningful-transition signal.
- **Layering multiple `window.on('close')` handlers doesn't work for guards.** Electron runs every registered 'close' handler even if one calls `e.preventDefault()`. The guard must live INSIDE the destructive handler so teardown only runs when the guard passes.
- **xterm.js `attachCustomKeyEventHandler` fires for BOTH `keydown` AND `keypress`.** Returning `false` only on `keydown` still lets `keypress` through, causing xterm to send `\r`. Must return `false` for both event types.

_Older Gotchas (16 entries: createWriteStream lazy-create, window.reload PTY leak, beforeunload race, wrapper .zshenv readonly, safeStorage timing, electron-store decrypt probe, onContextLoss runtime fire, dead IPC accumulation, shell validation drift, stty -echo timing, idle-detection duration guard, kitty Shift+Enter intercept, COLORFGBG + OSC 10/11 query response, tabBar.update() DOM rebuild race, ANSI light-theme contrast, dot-test inactive-tab gate, Claude Code "·" title prefix, Enter-in-/mcp app-side discriminator) archived 2026-05-07 S27 trim → `docs/session-archive.md`._

## Key Files
- `~/.claude/plans/see-this-screenshot-that-smooth-cake.md` — session-25 plan: atlas-eviction diagnosis, Phase 1 (periodic flush + Cmd+Shift+K + Capture Renderer State), and Phase 2 escalation (Canvas renderer fallback) if Phase 1 recurrence within the flush window.
- `~/Library/Application Support/Patton/logs/render-snapshots/<timestamp>-<reason>.json` — diagnostic snapshots written by S25's `captureSnapshot()`. Auto-fires on `resetRenderer()`; manually triggered via "Capture Renderer State" palette command. Schema: `RenderSnapshot` in `src/shared/types.ts`. Last 50 kept, rate-limited 5/s. If we ever ship logs upstream for an xterm.js bug report, these are the artefact to attach.
- `~/.claude/plans/see-the-screenshot-saved-glowing-kahn.md` — session-24 plan covering the render-corruption defensive hardening (A1–A4) and unified double-click links (B1–B4). Minimal-change rationales noted throughout.
- `~/.claude/plans/see-the-screenshot-saved-glowing-kahn-findings-render.md` — session-24 WebGL-atlas hypotheses ranked by confidence (atlas stale after theme swap > atlas stale after undetected context loss > `minimumContrastRatio` interaction), with instrumented tests to discriminate. Pick this up if the garbled-glyph state recurs and we still can't reproduce.
- `~/.claude/plans/see-the-screenshot-saved-glowing-kahn-findings-links.md` — session-24 full audit of Patton's link/URL/file-path handling surface: addon inventory, IPC channels, security-validation rules, regex semantics, and a concrete sketch of OSC 8 + context-menu + `editorCommand` deferred work.
- `src/renderer/services/file-link-provider.ts` — MD link + bare path regex, `event.detail === 2` gate, shared `pushLink()` with security rules (traversal rejection, cwd-relative resolution). Extend this file if OSC 8 or `editorCommand` land later.
- `~/.claude/plans/enchanted-sauteeing-moonbeam.md` — session-23 read-only input-path audit. Documents why Enter-not-working at Claude Code's `/mcp` auth prompt is not a Patton code-path issue, plus the Ctrl+C / letter-key discriminator and the keystroke-tracer fallback if it recurs.
- `~/.claude/plans/cached-strolling-dawn.md` — session-22 plan covering both the quit-confirmation fix and the dot-indicator rewire (three signal paths + polish). Useful if the feature needs to be re-analysed.
- `analysis/reasoning-modes/REPORT.md` (gitignored) — session-20 triangulated synthesis of 10 epistemic reasoning modes + UI/UX audit. 5 Kernel findings, 11 Supported, 10 Hypotheses, 2 Disputed. Individual mode reports under `analysis/reasoning-modes/reports/`. Run `/reasoning` again to refresh.
- `~/.claude/plans/image-1-my-interfaces-keep-tender-harp.md` — the original crash-hardening plan that kicked off session 20.
- `src/renderer/services/focus-trap.ts` — reusable modal focus trap. Import as `trapFocus(container)` → returns release fn.
- `src/renderer/services/announcer.ts` — global aria-live regions. Import as `announce(msg, 'polite' | 'assertive')`.
- `src/main/logger.ts` — file logger that tees `console.*` to `~/Library/Application Support/Patton/logs/main.log`. Called once from `main.ts` via `installFileLogger()`.
- `scripts/patton-health.sh` — diagnostic script. `npm run health` or `patton-health` (shell alias) from anywhere. Greps log file + crashpad for session-20 error markers.

## Next Steps
See `docs/NEXT_STEPS.md`.
