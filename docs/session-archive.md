# Patton — Session Log Archive

Older Session Log entries moved here to keep the primary CLAUDE.md under the 40k-character load-time threshold. Most recent sessions live in CLAUDE.md.

---

## 2026-04-18 (session 20) — Renderer-crash hardening pass

- Shipped renderer-crash hardening (Tier 1 + Tier 2 from the harden-patton-renderer-crashes plan): WebGL `onContextLoss` → DOM fallback, `render-process-gone` auto-reload, `unresponsive`/`responsive` breadcrumbs, `child-process-gone` logging for GPU/utility deaths, local-only `crashReporter` for post-mortem dumps.
- Ran a 10-agent epistemic review (`/reasoning`) plus UI/UX audit pass. Full report at `analysis/reasoning-modes/REPORT.md` (gitignored). 5 Kernel (HIGH-confidence) findings, 11 Supported, 10 Hypotheses, 2 Disputed.
- Worked through the **entire** resulting punch list in one session. Commit `34ce53e` covers 28 files, 683 insertions — grouped below.
- Crash resilience: crash counter (3-in-30s → static error page), `destroyByWindow()` before reload, session-clear after 2nd crash, `uncaughtException`/`unhandledRejection` handlers, WebGL 10s re-acquire retry + `refresh()` on fallback, synchronous PTY buffer flush in `destroy()`, graduated write-coalesce backoff (4/16/32ms tiers, 1MB hard cap).
- Security: store encryption replaced with random 32-byte key protected by `safeStorage` (Keychain) + legacy-key migration; modal confirmation on `startupCommand` change; `OnlyLoadAppFromAsar: true`; shell-setting allowlist alignment; `readonly` guard on `PATTON_SHELL_INTEGRATION_SCRIPT` in wrapper `.zshenv`; 100ms OSC 10/11 debounce; history control-char stripping; session-tree leaf cap; notification length caps + tabId validation; expanded env allowlist (CONDA, KUBECONFIG, ANDROID, DOTNET, AWS, GCLOUD, VIRTUAL_ENV, etc.).
- Command-done coordination: per-pane `fireCommandDone(source)` with 5s cross-source debounce; OSC 133 suppresses idle heuristic for 60s after any fire. Kills triple-notification.
- Session persistence: 30s periodic autosave (covers `beforeunload` teardown race); startup command waits for OSC 133;B prompt signal (1.5s fallback) instead of blind 500ms timer.
- Accessibility: `<html lang="en">`, global `prefers-reduced-motion`, reusable `services/focus-trap.ts` applied to 5 modals, quick-terminal ARIA + `aria-hidden` toggle, command-palette `id`+`aria-activedescendant`+combobox roles, tab-bar Arrow/Home/End roving-tabindex nav, global `services/announcer.ts` (aria-live) wired to tab ops / broadcast / settings / paste. Theme tokens `--broadcast`, `--prompt-ready`, `--prompt-ready-glow` replace hardcoded hex. Distinct "No command history yet" vs "No matches" empty states.
- Dead-code cleanup: removed `PTY_GET_DESCENDANTS` IPC chain (5 files, always returned `[]`).

## 2026-04-15 (session 19) — Light-theme contrast fix

- Fixed persistent light-theme text readability — CLI tool text (Claude Code, etc.) was unreadable on light backgrounds despite COLORFGBG and OSC 10/11 fixes from prior sessions.
- Root cause: xterm.js `minimumContrastRatio` was unset (defaults to 1 = disabled). VS Code uses the same ANSI palette but sets this to 4.5, which auto-corrects low-contrast colors. Patton copied the colors without the safety net.
- Fix 1: Added `minimumContrastRatio: 4.5` to Terminal constructor — xterm.js now auto-adjusts any foreground color below 4.5:1 contrast against the background.
- Fix 2: Replaced 6 ANSI colors in LIGHT_THEME that had terrible inherent contrast (e.g. `brightWhite` was 2.4:1, `brightGreen` was 2.0:1 — WCAG AA requires 4.5:1).
- Previous sessions: OSC 10/11 responses (18), tab rename fix (17), COLORFGBG env var (16).

## Session Log entries archived S27 trim (2026-05-07)

### 2026-04-20 (session 23) — input-path audit (no code changes)

- Diagnostic triggered by Enter appearing unresponsive at Claude Code's `/mcp` "Press Enter after authenticating" step. User confirmed Enter works in Claude Code's main prompt in the same session (messages sent to Claude), so any regression would be Enter-path-specific, not a blanket keystroke issue.
- Audited every Enter handler in the renderer. **`pane.ts:164-174`** (`attachCustomKeyEventHandler`) intercepts **only Shift+Enter** to emit the kitty `\x1b[13;2u` sequence — plain Enter returns `true` so xterm.js handles it natively (sends `\r` to the PTY). **`editor-input.ts:55-73`** captures Enter for compose-panel submit, but only when the textarea holds focus. All other Enter handlers (`context-menu.ts:46`, `paste-dialog.ts:45`, `history-search.ts:115`, `onboarding.ts:53`, `command-palette.ts:79`, `search-overlay.ts:69-72`, `tab-bar.ts:283`) are focus/open-gated on their respective modal. Conclusion: no Patton code path selectively swallows plain Enter in a Claude Code subscreen.
- Checked xterm.js v6.0.0 for kitty keyboard progressive-enhancement support — no `registerCsiHandler` for CSI-u and no DECSET 2017 registration in the codebase (grep confirmed). Patton never ACKs kitty mode, so Enter always resolves to `\r` regardless of what the TUI app requests. Rules out the hypothesis that Claude Code's `/mcp` expected `\x1b[13u`.
- Flow auto-completed via server-side OAuth poll before user could run a discriminating Ctrl+C / letter-key test, so the original repro is lost. Left unresolved — most likely Claude Code's auth-poll loop isn't reading stdin during the poll window; watching brief only.
- Investigation notes at `~/.claude/plans/enchanted-sauteeing-moonbeam.md`. No code changes this session; CLAUDE.md update only. [no-release] candidate.

### 2026-04-18 (session 22)

- **Rewired tab "awaiting input" dot through three signal paths** — old OSC 133 `'prompt'` subscription fired on every prompt redraw (startup, session restore, SIGWINCH, `clear`, Starship refresh), so the dot never actually appeared for meaningful events. Fix routes SET through (A) `onCommandDone` callback (session-20's `fireCommandDone()` coordinator), (B) title-prefix regex `/^[·•]/` on `onTitleChange`, (C) new OSC 9 handler → `fireCommandDone('osc9')`. All three converge on `raiseAwaitingInput()`. CLEAR paths unchanged.
- **Visual polish**: 8 → 10 px dot, dual box-shadow (2 px + 8 px), 1.8 s opacity pulse. Global `prefers-reduced-motion` zeroes the animation.
- **Confirm-before-quit dialog** for the class of event that lost three live Claude Code sessions (genome, lindus-therapeutics, new-house). Two hooks: `before-quit` (⌘Q) + `window.on('close')` (⌘W-to-last-window) because `before-quit` fires after PTY teardown. Uses `dialog.showMessageBoxSync` (async would race app teardown). Scope-expanded mid-plan to cover the close-last-window path.
- 3 commits (`f600248` + `39ed72a` + `d54ceb8`), ~135 LOC / 7 files. Patch release via CI. Verified strings in asar + Path A live-tested.

### 2026-04-18 (session 21)

- **Tab "awaiting input" indicator** shipped (iTerm-style green dot on inactive tabs at OSC 133 prompt-ready). Reuses `--prompt-ready` tokens for visual consistency. Plumbing: `tab.ts` state + subscription → `tab-manager.ts` → `tab-bar.ts` dot element. (Reworked in S22 — signal was wrong; this session wired the wrong SET path.)
- **File logger** (`src/main/logger.ts`, ~90 LOC): tees `console.*` to `~/Library/Application Support/Patton/logs/main.log` with 5 MB rotation. Eagerly creates file + startup line on install. Fixes the session-20 observability blind spot (Dock-launched Electron stderr goes to `/dev/null`).
- **Health script** (`scripts/patton-health.sh`, `npm run health`): one-shot diagnostic — counts crash dumps, greps session-20 error markers, verifies `config.json` + `key.enc`, `--hours=N` window works across log rotations.

## Decisions archived S27 trim (2026-05-07)

Pre-S24 Decisions (S20–S23 vintage). Kept verbatim — they remain context for any future change in the same area.

- **2026-04-20 — Enter unresponsive at Claude Code `/mcp` "Press Enter" is not a Patton code-path issue.** Verified by read-only audit of every Enter handler in the renderer. Only Shift+Enter is intercepted (`pane.ts:166-174`); compose-panel Enter is focus-gated; xterm.js v6 registers no kitty/CSI-u handler that could mutate Enter based on mode. Main-prompt Enter works in the same Claude Code session, so keystrokes ARE reaching the PTY. Repro lost before a Ctrl+C / letter-key discriminator could run — no fix proposed; watching brief only. If it recurs with any key (not just Enter), that reclassifies it as a Patton focus/keystroke-delivery bug and we add a dev-toggleable keystroke tracer to `terminal-view.ts`.
- **Dot fires on meaningful signals, not "shell at prompt"** — OSC 133 `'prompt'` fires on every prompt redraw (startup / SIGWINCH / `clear` / Starship refresh / session restore). Using it as a SET signal meant tabs running non-OSC-133 tools (Claude Code, npm, docker) never lit, and every plain-shell tab lit whenever the shell redrew. Rewired to set on command-done transitions (Path A), Claude Code's OSC 2 title prefix (Path B), and OSC 9 terminal notification escape (Path C). Three independent paths converge on a single `raiseAwaitingInput()` guard. CLEAR on tab activation + new command start, same as before.
- **Two quit-confirmation surfaces (before-quit + window close)** — Electron's `before-quit` fires after `window-all-closed`, by which point the existing `window.on('close')` handler has already destroyed PTYs via `destroyByWindow`. A single `before-quit` guard would therefore bypass the ⌘W-to-last-window path entirely. Rather than reorder PTY cleanup into a more complex state machine, I added a second dialog in `window-manager.ts` gated on `isLastWindow && ptyManager.size > 0`. Two surfaces share one UX; the second catches what the first can't see.
- **`dialog.showMessageBoxSync` deliberate** — `before-quit` doesn't await promises, and an async dialog would race app teardown. Sync blocks the main process until the user chooses, which is the required semantic.
- **Confirm for ANY live PTY, not just "busy" ones** — the main process doesn't know pane-level OSC 133 state; plumbing that through IPC just to silence the dialog for idle shells isn't worth the complexity. False-positive annoyance < silent data loss.
- **Consolidate the two close handlers instead of layering** — Electron fires all registered 'close' handlers even if one calls `e.preventDefault()`, so stacking a guard handler on top of the destroy-PTYs handler would kill PTYs before the user answered. Merged the guard into the existing close handler so the teardown only runs if the guard passes.
- **Accept visual redundancy of our dot + Claude Code's "·" title** — they don't visually collide (dot is tab chrome, "·" is inside title text). Suppressing ours when title begins with "·" would be a fragile contract with Claude Code's conventions.
- **File logger eagerly creates the log file on install, not lazily on first write** — first attempt only opened the stream on the first `console.*` call. On a fully clean session (no warns, no errors) nothing ever called `console.*`, so the file never materialized. Made the health script indistinguishable between "logger not installed" and "session ran clean." Writing a "Patton main process started" line on install guarantees the file exists after every launch.
- **Tab indicator shares prompt-ready theme tokens instead of its own color** — considered a distinct accent (blue, cyan) but picked `--prompt-ready` because the editor's prompt-ready glow uses the same signal. One visual language for "shell is ready for input" across surfaces; one token to tweak if the hue is ever wrong.
- **Dot suppressed on the active tab, not just styled differently** — active tab is where the user's attention already is, so a dot would be noise. Cleared on `show()` even if the pane re-enters `'prompt'` a moment later; `handlePaneStateChange` guards on `!this._isActive` so it can't be set while active.
- **safeStorage-backed random 32-byte key over deterministic SHA-256** — previous SHA-256(homedir:username) was reconstructable by any same-user process; new key lives in `userData/key.enc` encrypted via OS keychain. Legacy-key migration on first launch; fallback to legacy if `safeStorage` unavailable (Linux without GNOME keyring).
- **CommandDoneCoordinator (coordinate, don't eliminate)** — OSC 133;D authoritative when shell integration is on, idle heuristic covers non-OSC-133 children (Claude Code, npm, docker compose), bell is explicit. Per-pane coordinator debounces 5s across all three; OSC 133 suppresses idle 60s after fire.
- **Crash circuit breaker 3-in-30s → static error page** — below: transient GPU blip recovers silently. Above: deterministic cause loops forever. Session clear after 2nd crash breaks restore-triggered loops before hard cap.
- **Periodic 30s autosave over change-triggered** — single call-site vs 5+ wire points. Trades lsof roundtrip for coverage certainty.
- **Startup command waits for OSC 133;B, not a fixed timer** — slow shells exceed the old 500ms; readiness-based listener + 1.5s fallback for shells without integration.
- **Shell allowlist single-sourced (`ALLOWED_SHELLS` exported from `pty-manager.ts`)** — previous permissive regex in `store.ts` + strict set in pty-manager let users save a shell that silently fell back at spawn. Always consolidate security allowlists.
- **Crashpad `uploadToServer: false`** — local dumps only. Post-mortem diagnosis without any network channel.
- **ZDOTDIR for zsh / `--rcfile` for bash shell integration** — VS Code's pattern: custom init script restores user's startup files then sources integration. Avoids PTY write echo.
- **Kitty keyboard protocol for Shift+Enter** — sends `\x1b[13;2u` so apps can handle Shift+Enter as "newline without submit". Chosen over backslash-continuation (shell-only) and compose-panel-open (too disruptive).
- **COLORFGBG from renderer + OSC 10/11 responses** — env var for startup detection, escape-sequence response for runtime detection. CLI apps need both to pick readable colors.
- **minimumContrastRatio: 4.5** — xterm auto-adjusts fg colors below this contrast against bg. Same approach as VS Code. Palette fix alone is insufficient for 256-color / true-color values from CLI apps; ratio is the safety net.

## Gotchas archived S27 trim (2026-05-07)

Older Gotchas, preserved for context. The recurring / load-bearing ones (and S25/S27 family) stay in CLAUDE.md.

- `createWriteStream(..., { flags: 'a' })` does NOT create the file on construction; it creates on first write. A file-based logger that lazy-opens will never create the file on completely clean sessions where nothing logs. Eagerly write a "started" line on install.
- `window.reload()` does NOT destroy main-process PTYs. Without an explicit `ptyManager.destroyByWindow(window)` before reload, each crash-recovery reload leaks PTYs — they keep running in main, sending PTY_DATA for ids the new renderer doesn't recognize, eventually hitting `MAX_PTY_PER_WINDOW`.
- `beforeunload` is fire-and-forget and does NOT fire on renderer crash. A periodic autosave (30s) is the only way to keep persisted state close to reality for crash recovery.
- wrapper `.zshenv` in ZDOTDIR mode sources user's `.zshenv` AFTER ours runs but BEFORE Patton's integration script. A compromised user `.zshenv` can redirect `PATTON_SHELL_INTEGRATION_SCRIPT` to a malicious path unless we `typeset -r`/`readonly` the captured value first.
- Electron's `safeStorage` is only available after `app.ready`. Store must be lazy-initialized (first access inside `app.on('ready', ...)` is fine; at module load would fail).
- `electron-store` doesn't expose a "try decrypt" probe — any mismatch throws. Migration has to instantiate a temp store with the legacy key, snapshot `store.store`, delete the file, then reinit with the new key.
- xterm.js WebGL addon fires `onContextLoss` at runtime (sleep/wake, display unplug, GPU driver reset). A plain `try { loadAddon }` catches sync load errors only — runtime loss will take down the whole renderer unless you subscribe to `onContextLoss` and `dispose()` the addon (falls back to DOM). Same pattern as `minimumContrastRatio` — VS Code does it, copying only configuration without the safety nets is a trap.
- Dead IPC surfaces accumulate. `PTY_GET_DESCENDANTS` returned `[]` for months with no callers but was still exposed via preload — potential attack surface. Periodically grep for IPC handlers and confirm callers exist.
- Shell validation drift: `store.ts` had a permissive regex (`/^\/[a-zA-Z0-9/._-]+$/`) while `pty-manager.ts` enforced a tight `ALLOWED_SHELLS` Set. User could save `/usr/local/bin/fish`, see it persist, silently get the default. Always single-source security-relevant allowlists.
- `stty -echo` on the same line as `source ...` does NOT prevent echo — the terminal driver echoes characters as they arrive, before the shell executes anything. Must inject via shell startup (ZDOTDIR, --rcfile) not PTY write.
- Idle detection fires on ANY PTY data, including prompt redraws and background trickle. Duration guard is essential — byte count alone isn't enough since small periodic output accumulates.
- xterm.js treats Shift+Enter identically to Enter (`\r`) by default. Must use `attachCustomKeyEventHandler` to intercept and send a distinct escape sequence.
- xterm.js `attachCustomKeyEventHandler` fires for BOTH `keydown` AND `keypress`. Returning `false` only on `keydown` still lets `keypress` through, causing xterm to send `\r`. Must return `false` for both event types.
- CLI apps (Claude Code, etc.) use `COLORFGBG` env var to detect light vs dark terminal backgrounds. Without it, they assume dark and render light-colored text — invisible on light backgrounds. Must set at PTY spawn time; can't change env vars for running PTYs.
- CLI apps also query terminal colors at runtime via OSC 10/11 escape sequences (`\x1b]10;?\x07` and `\x1b]11;?\x07`). xterm.js doesn't respond to these by default — must hook `onData` and write back `rgb:RR/GG/BB` responses.
- `tabBar.update()` rebuilds the entire DOM (`innerHTML = ''`). Any click-triggered callback that calls `updateTabBar` will destroy elements mid-interaction (e.g., breaking `dblclick` because the first click's target is gone). Guard with early-returns for no-op state transitions.
- Light terminal ANSI colors need explicit contrast checking. xterm.js default `minimumContrastRatio` is 1 (disabled). Colors like `#a5a5a5` (brightWhite) or `#14ce14` (brightGreen) are invisible on white. VS Code uses the same palette but enables contrast enforcement — copying just the colors without the setting is a trap.
- **OSC 133 `'prompt'` is not "awaiting input".** It fires on every prompt redraw — shell startup, session restore, SIGWINCH, `clear`, async-prompt theme refreshes (Starship, Powerlevel10k). Using it as the SET signal for an attention indicator means every inactive tab lights up ~always.
- **Layering multiple `window.on('close')` handlers doesn't work for guards.** Electron runs every registered 'close' handler even if one calls `e.preventDefault()` — a guard handler registered before a destructive one can't prevent the destructive one from running. The guard must live INSIDE the destructive handler.
- **Claude Code's "·" tab-title prefix is text INSIDE the title (OSC 2), not a chrome element.** The user visually conflates it with any indicator Patton renders near the title. Visual amplification of Patton's own indicator (size / glow / pulse) is load-bearing so the two are distinguishable at a glance.
- **Testing that the dot fires requires the signal to arrive while the tab is INACTIVE.** `raiseAwaitingInput()` gates on `!_isActive`. Use a delayed command (`sleep N && <signal>`) and switch tabs during the delay.
- **Packaged Electron apps launched from Dock/Finder route main-process stderr to `/dev/null`.** Without a file logger, every hardening signal is invisible. See `src/main/logger.ts`.
- **Silent ENOENT in `main.log` for days before detection.** Before session 24, `spawn code ENOENT` had been logging 20+ times across 4 days because `code` wasn't on PATH. Worth a periodic log-scan in `scripts/patton-health.sh` or a session-start greeting that counts recent warnings.
- **xterm.js `ILink.activate(event, text)` receives the native `MouseEvent` — but whether `event.detail === 2` reliably fires for double-click is unverified as of session 24.** If double-click appears to do nothing, the fallback is a native `dblclick` listener on `terminal.element` that hit-tests against the active link range.
- **`setCustomTheme()` is called BEFORE `mount()` on new tabs that inherit the current theme** (`tab-manager.ts:153-155`). Any WebGL-aware code in setCustomTheme must guard `if (this.webglAddon)` — otherwise it'll try to dispose/reload an addon that doesn't exist yet.

## Session Log entries archived S28 trim (2026-05-31)

_The S24–S27 render-corruption saga closed in S28 (shipped as v1.4.0, validated in real use — zero recurrence since ~2026-05-18). Full Session Log entries for S24–S26 relocated here; S27 retained as a trimmed entry in CLAUDE.md._

**2026-04-27 evening (session 26)** — third recurrence diagnosed: pre-S25 binary still in memory
- User reported third recurrence at ~20:25 BST today (`~/Downloads/Screenshot 2026-04-27 at 20.25.31.png`). Same signature as S25's diagnosis: older content shredded into Latin-Extended-A + combining diacritics, recent lines partly intact, window-resize recovered (resize forces reflow + re-rasterize, partially bypassing stale atlas — consistent with eviction theory). User: "Why does this keep happening?"
- Diagnosis (no code work): running Patton process PID 61998 started **08:16 BST**; `/Applications/Patton.app/Contents/Resources/app.asar` rebuilt at **08:24 BST** (S25's `npm run package` for commit `89404e4`). Process is on the PRE-S25 in-memory binary. macOS doesn't hot-swap an `.app` after reinstall; ⌘Q + relaunch required.
- Confirmed via `~/Library/Application Support/Patton/logs/main.log`: zero `[RENDER] periodic atlas flush` lines anywhere → S25's `setInterval` is not running. Last `Patton main process started` line is `2026-04-27T07:16:52Z` (= 08:16 BST). No `[RENDER] WebGL context lost` lines either, consistent with S25's diagnosis (gradual saturation, not bursty context loss).
- No code changes. User relaunching Patton; S25 fix loads on next launch. Validation clock for the 5–7 day Phase 1 watch starts at relaunch, not at the original commit timestamp.
- **Lesson**: "fix shipped to disk" ≠ "fix loaded in memory" for Electron asar builds. Strengthened the existing macOS hot-swap Gotcha with a concrete detection recipe (`ls -lt …/app.asar` mtime vs `ps -o lstart= -p <pid>`). Same gotcha bit S20-era debugging too; this is its third surfacing — the diagnostic recipe should keep it from costing future sessions.

**2026-04-27 (session 25)** — atlas-eviction root cause + Phase 1 fix shipped locally
- **Recurrence**: garbled-glyph render bug recurred ~22:00 on 2026-04-26 (screenshot at `~/Downloads/Screenshot 2026-04-26 at 22.02.15.png`). S24's atlas-flush-on-theme-swap and GPU breadcrumbs were in the running build (asar mtime 2026-04-24 18:43) yet `main.log` had ZERO `[RENDER] WebGL context lost` entries since S24 shipped, and no theme swap occurred during the live session. Both S24 hypotheses (theme-swap atlas staleness, undetected context loss) disconfirmed by the recurrence.
- **Diagnosis**: screenshot signature highly diagnostic — bullets / numbers / recently-rendered prompt render correctly; older paragraphs garbled into Latin-Extended-A + combining diacritics; bold red headers partially correct. Pattern fits xterm.js's WebGL atlas LRU eviction: when the fixed-size atlas saturates under glyph pressure, old slots get reassigned to new glyphs, but buffer cells that referenced those slots are NOT repointed. Long Claude Code sessions are the trigger — diverse glyph variants (coloured bullets, box-drawing, bold/italic/dim combos, many fg×bg pairs) saturate the atlas. xterm.js bug, not Patton bug, but Patton can mitigate.
- **Phase 1 (commit `89404e4`, 155 LOC across 7 files, NOT pushed)**:
  - `terminal-view.ts`: periodic `clearTextureAtlas()` every 10 min via setInterval; logs `[RENDER] periodic atlas flush` per fire; cleared on `dispose()`. `clearTextureAtlas()` is a public xterm 6 API (no addon disposal needed — much cheaper than S24's setCustomTheme dispose+reload path).
  - `terminal-view.ts`: atlas flush on `clear()` (Ctrl+L / `clear` / palette Clear) — natural pressure-release boundary, user already expects a fresh visual state.
  - `keybinding-manager.ts`: Cmd+Shift+K hotkey for Reset Renderer (1 keystroke vs 4 via palette). Verified unbound elsewhere via grep.
  - `terminal-view.ts`: `captureSnapshot(reason)` method writes JSON via new `diagnostics.saveSnapshot` IPC. Auto-fires inside `resetRenderer()` BEFORE the flush so captured state reflects the bug, not the recovery.
  - `app.ts`: new "Capture Renderer State" palette command + `⌘⇧K` shortcut hint on Reset Renderer entry.
  - `ipc-handlers.ts` + `preload.ts` + `constants.ts` + `types.ts`: `DIAGNOSTICS_SAVE_SNAPSHOT` IPC writing JSON to `~/Library/Application Support/Patton/logs/render-snapshots/<timestamp>-<reason>.json`. Last 50 kept; rate-limited 5/s; 200KB scrollbackTail cap.
- **Build state**: `npm run lint` clean, `npm run package` clean, asar verified to contain `periodic atlas flush`, `Capture Renderer State`, `render-snapshots`, `reset-renderer`, `capture-render-state`, `RENDER] snapshot`. `/Applications/Patton.app` rebuilt with new code; running Patton process retains OLD binary in memory until ⌘Q + relaunch (per the macOS asar gotcha). Commit local-only; CI auto-release not triggered.
- **Live diagnostic step proposed but not executed**: ⌘⇧P → Reset Renderer on the garbled tab to confirm the atlas-eviction hypothesis without restarting. User reported Patton crashed mid-session before the test could run — likely the corruption escalated, unrelated to my changes (running process loaded the OLD binary and never saw the new code). On relaunch, user gets the fixed build automatically. Hypothesis remains plausible but technically unconfirmed; Phase 1's periodic flush is a pure win regardless (no harm if hypothesis is wrong, the flush is cheap).
- **User confusion mid-session about "what is being pushed"**: clarified that all changes are local-only; "push" = publish to GitHub releases / Homebrew (only needed if propagating to other machines). Local laptop fix is done — relaunch is sufficient.
- _Plan reference_: `~/.claude/plans/see-this-screenshot-that-smooth-cake.md` (includes Phase 2 Canvas-renderer escalation if Phase 1 still recurs).

**2026-04-24 (session 24)** — render-corruption hardening + unified double-click links
- **Trigger**: user screenshot showed Claude Code's tab output rendering as overstruck Latin-Extended + combining-diacritic glyphs (`Ā À ē Ă`) across several screenfuls — table borders and bottom italic-gray recap rendered fine, only red/bold text-content in cells was garbled. One-off, no known trigger. Log scan ruled out WebGL context-loss events, crash dumps, renderer-gone events. Same session: user asked for MD-link / file-path clickability matching Claude Code chat's double-click UX.
- **Render hardening (commit `968870e`)** — top hypothesis for the garble was stale WebGL glyph atlas after a theme swap, since `setCustomTheme()` was mutating `terminal.options.theme` without rebuilding the atlas. Changes: `loadWebgl()` extracted from `mount()` into a named method, `webglAddon` tracked on the TerminalView instance, `setCustomTheme()` now disposes+reloads the addon so the atlas rebuilds under the new colors, public `resetRenderer()` added as the user's escape hatch, new **Reset Renderer** command-palette entry dispatches it against the focused pane, `onContextLoss` log upgraded to `[RENDER] WebGL context lost` with `tryGetGpuInfo()` (WEBGL_debug_renderer_info vendor/renderer) + theme flag + ptyId. Explicitly NOT touched: `minimumContrastRatio: 4.5` (would regress session-19 contrast fix) and `FONT_FAMILY` (SF Mono / Menlo ship bold variants on macOS, fallback theory unfounded).
- **Double-click links (commit `c19f1a4`)** — WebLinksAddon handler now gates on `event.detail === 2`. `FileLinkProvider` rewritten: new pass for markdown `[text](path)` (whole span clickable, http(s) targets deferred to WebLinksAddon), existing bare-path pass skips ranges already claimed by markdown links on the same line, `activate(event)` receives the MouseEvent and gates on double-click. Single clicks fall through to xterm's selection so drag-select over path-containing text still works.
- **Editor fix (commit `3118108`)** — replaced bare `execFile('code', ...)` with a priority cascade: `/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code` if present at handler-install time → `code` on PATH → `shell.openPath()` with `:line:col` stripped. Caught because `main.log` had 20+ ENOENT entries over 4 days that nobody had noticed (no VS Code installed on this machine at all — `/usr/bin/vim` only). Now falls back to macOS default opener for `.md` and friends.
- **Released**: `v1.1.21` (editor patch), `v1.2.0` (links — `feat(` triggered minor), `v1.3.0` (render — also minor per release script's heuristics). Local `/Applications/Patton.app` rebuilt via `npm run package`; verified the new code is in the asar (minified `a.detail!==2` present, rewritten `FileLinkProvider` with `MD_LINK_RE` present). ⌘Q + relaunch required to pick it up.
- **Scope control**: the plan explicitly deferred OSC 8 hyperlink support, context-menu integration, an `editorCommand` setting, and double-click-vs-single-click-per-protocol hybrid UX — all noted in the link-audit findings file for a future session.

## Decisions archived S28 trim (2026-05-31)

- **2026-04-24 (S24) — Defensive hardening for the render-corruption bug, not reproduction-first diagnosis.** User report was a one-off screenshot, no crash, no WebGL context-loss in the log. Top hypothesis (stale atlas after theme swap) is plausible but not proven; proving it would require reproducing a bug the user can't reproduce. Instead shipped: atlas-flush on theme change (the most plausible defensive fix, near-zero cost, one-frame flicker on rare theme changes) + a user-triggered Reset Renderer command for when the state recurs + GPU breadcrumbs to catch the next occurrence with more signal. Accepts that the bug may still recur — we'd rather give the user a one-keystroke escape hatch than ship a speculative "fix" that might be wrong.
- **2026-04-24 (S24) — Unified double-click across ALL link types** (URLs + file paths + markdown), not the hybrid VS Code/iTerm convention (Cmd+click for files, single-click for URLs). User explicitly picked this to match Claude Code chat. Downside: URLs require double-click now (muscle-memory change from standard browser behavior). Upside: no stray opens while selecting text, consistent mental model across all link types, matches the tool the user spends the most time in.
- **2026-04-24 (S24) — `event.detail === 2` gate over custom `dblclick` listener.** Minimal-change path: xterm.js forwards the native MouseEvent to link-provider activate, and Chromium's `detail` is a reliable click-count. If empirically detail always resolves to 1 (xterm may collapse gestures), the fallback is a native `dblclick` listener on `terminal.element` with hit-testing against the active link range — but that's additional code we shouldn't pre-emptively write. Try the minimal path first.
- **2026-04-24 (S24) — VS Code resolved via app-bundle path before PATH lookup.** Most users who install VS Code don't run "Shell Command: Install 'code' command in PATH" inside the app. Resolving `/Applications/Visual Studio Code.app/Contents/Resources/app/bin/code` directly catches the majority case without any manual setup. Still falls through to `code` on PATH so Cursor/Codium/fork CLIs that expose a `code` shim keep working.
- **2026-04-24 (S24) — `shell.openPath` final fallback, not silent ENOENT.** Previous behavior logged ENOENT and opened nothing; 20+ silent failures accumulated in `main.log` over 4 days before being noticed. Now falls through to the macOS default-app opener (Preview.app for `.md`, etc.) so the user gets *something* even without a code editor. Loses `:line:col` — acceptable trade: "opens" beats "silently failed".
- **2026-04-24 (S24) — Reset Renderer is user-triggered (palette command), not auto-applied on suspicion.** Considered auto-calling `resetRenderer()` on some heuristic (e.g. scheduled flush every N minutes, or on detecting suspicious character sequences). Rejected: over-eager auto-flushing in the working case burns a frame for no benefit and masks the underlying signal. Making it user-triggered means we actually learn whether the recovery works the next time the bug recurs.
- **2026-04-24 (S24) — Theme swap disposes+reloads the WebGL addon synchronously, not deferred.** Considered a debounce (batch multiple rapid theme changes into one flush), but theme changes are rare (1-2 per hour at most) and a one-frame flicker per change is imperceptible. The immediate rebuild also guarantees the atlas never carries stale colors even for a single frame. KISS.

## Decisions archived S30 trim (2026-06-08)

- **2026-05-07 (S27) — `backgroundThrottling: false` is the right root-cause fix, not a workaround.** The user's reported trigger (Cmd+Tab away, come back) maps directly to Chromium's renderer-throttling behaviour on app background. With throttling enabled (default), the renderer's timers slow and parts of the GPU pipeline can degrade — atlas state ends up stale on resume. Disabling throttling keeps the renderer fully alive in background. Trade-off: slightly higher background CPU/battery for a terminal that's mostly idle anyway. Acceptable. Considered: leaving throttling on and relying solely on the visibility-flush handler — rejected because that catches the corruption AFTER it happens; the throttling pin prevents it.
- **2026-05-07 (S27) — Both `visibilitychange` and `window.focus` listeners.** macOS doesn't fire both consistently for every focus-change shape (Cmd+Tab vs click-on-window vs Mission Control). Listening to both is cheap (clearTextureAtlas() is a no-op when nothing's wrong) and covers all triggers. 1s throttle prevents thrashing on rapid Cmd+Tab spam. Considered: just `window.focus` — rejected because Chromium fires `visibilitychange` in some macOS sleep/wake paths where `focus` doesn't.
- **2026-05-07 (S27) — Auto-reload on flush error, not just log-and-continue.** S25's flush path swallowed errors with a warn; in practice, if `clearTextureAtlas()` throws, the GL context is wedged and a flush won't fix anything. Disposing+reloading the addon mirrors the user-triggered Reset Renderer recovery, which we know works. The reload is silent (no snapshot, no UI nag) because background paths shouldn't punish the user for self-healing.
- **2026-05-07 (S27) — Renderer log forwarding via IPC, not by replacing renderer console globals.** Considered monkey-patching `console.info` etc. in the renderer (mirroring main's logger pattern). Rejected: would break DevTools console-formatting plugins, complicate test setup, and obscure where logs originate. Explicit `rlog()` at the call sites is surgical — every `[RENDER]` line in `terminal-view.ts` was already a deliberate log; the cost of switching them to `rlog` is the same as keeping them on `console`.
- **2026-05-07 (S27) — `document.hasFocus()` not `document.hidden` for notification gating.** With `backgroundThrottling: false`, the Page Visibility API reports 'visible' even when the app is occluded, so `document.hidden` becomes useless as a "is the user looking at me?" signal. `document.hasFocus()` is the actual signal we want and is unaffected by the throttling pin. Surfaced as a known pitfall in Gotchas — anyone touching the notification path needs to know about this.
- **2026-05-07 (S27) — Periodic flush interval 10 → 2 min, not 5 or 1.** The visibility/focus handlers should catch the user's actual trigger directly. The periodic flush is now a safety net for non-focus-change atlas saturation (gradual pressure under unusually long output bursts). 2 min is aggressive enough to limit dwell time in any wedged state but cheap enough that the log volume stays manageable (≤30/h/pane, ≤150 entries/h with 5 panes — well within 5MB rotation). 1 min would log too much; 5 min reverts to roughly S25's behaviour.
- **2026-05-07 (S27) — Single bundled commit, not split across defenses.** The fixes are coupled: backgroundThrottling forces the document.hidden → hasFocus migration, the visibility handler depends on the new rlog helper for visibility-flush observability, the IPC channel is needed by the renderer logger. Splitting would create commits that don't individually pass real-use validation. Bundled is the right granularity here.
- **2026-04-27 (S26) — Recurrence on pre-S25 binary is NOT a Phase 2 escalation trigger.** Phase 2 (Canvas renderer fallback) was scoped to fire if recurrence happens *inside the 10-min flush window* — which requires the periodic flush to actually be running. A recurrence on the pre-fix binary is just the original bug doing what it always did; nothing about today's screenshot disconfirms S25's diagnosis or fix. Validation clock restarts at relaunch.
- **2026-04-27 (S26) — Diagnostic-only checkpoint, no Patton code commit.** S26's deliverable is the CLAUDE.md update (strengthened Gotcha + Session Log). The recommendation to ⌘Q + relaunch isn't a code change. Considered: forcing ⌘Q from a script — rejected, the user owns when terminal sessions die. Push of `89404e4` to GitHub remains gated on user's choice (S25 decision unchanged).
- **2026-04-27 (S25) — Periodic preventive `clearTextureAtlas()` over reactive triggers.** A `setInterval` 10-min per pane is the simplest mitigation that addresses the actual cause (gradual atlas saturation under sustained pressure), not the bursty triggers S24 hypothesised (theme swap, context loss). Cheap: `clearTextureAtlas()` is a public xterm 6 API that just clears the cache — xterm rebuilds glyphs lazily on next render, no addon disposal needed (vs S24's setCustomTheme dispose+reload path). Considered: per-prompt-redraw flush (too frequent, would stutter on heavy output bursts), per-OSC-133;D flush (too rare for non-OSC-133 sessions like Claude Code's child PTYs).
- **2026-04-27 (S25) — Auto-snapshot on `resetRenderer()`, captured BEFORE the flush.** The user invokes Reset Renderer when they SEE the bug; snapshotting before the flush captures the moment of corruption. Snapshot AFTER would only show the recovery state. Diagnostic value lives in the bug state, not the recovery state.
- **2026-04-27 (S25) — Defer Phase 2 (Canvas renderer fallback) to "if Phase 1 recurs".** Phase 2 adds dependency + settings UI + per-renderer code paths — meaningful complexity for a fallback we may not need. Phase 1 is well-targeted at the actual root cause; escalate only if 5–7 days of validation shows recurrence inside the periodic-flush window. Loose framing: pursuing root-cause-fitting changes first preserves WebGL's perf advantage for the majority case.
- **2026-04-27 (S25) — Single commit (155 LOC) instead of plan's proposed three commits.** Splitting `terminal-view.ts` across two commits would require git add -p of ~30 lines each — not worth the ceremony for a hot bug. Rollback target unchanged either way. CI auto-release threshold (patch ≤500 LOC) is well clear of the single-commit size.
- **2026-04-27 (S25) — S24 watching brief on render bug `[~] SUPERSEDED`.** The (b) garbled-glyphs question is resolved (recurred → diagnosed → fixed); the (a) double-click question carries forward as a separate Next Step. The original "watching brief" shape no longer fits — it's now a confirmed bug + fix pending validation.

## Gotchas archived S30 trim (2026-06-08)

_The two atlas-eviction trigger gotchas below were merged into one concise entry in CLAUDE.md at S30 (DOM is now the default renderer, so they only apply to WebGL opt-in users); full text preserved here._

- **xterm.js WebGL atlas eviction is one of (at least) two distinct "garbled glyphs" triggers** — saturation-driven (S25 diagnosis: long Claude Code sessions emit many glyph variants, fixed-size atlas LRU-evicts slots, buffer cells aren't repointed) AND focus/refocus-driven (S27 diagnosis: Chromium suspends renderer in background, GPU state silently corrupts). Symptom signature is identical from the user side; only the trigger differs. Periodic `clearTextureAtlas()` (S25) addresses saturation, `backgroundThrottling: false` + visibility/focus flush (S27) addresses the focus path. Both are needed; don't remove either thinking the other handles all cases.
- **xterm.js WebGL atlas eviction is the recurring "garbled glyphs" trigger** (S25 root-cause). Long sessions with diverse glyph variants (coloured bullets, box-drawing, bold/italic/dim, many fg×bg combos) saturate the fixed-size atlas; LRU evictions reassign slots without repointing buffer cells, so historical scrollback shows wrong characters while recent output renders fine. Recovery as of S25: ⌘⇧K (Reset Renderer) or wait up to 10 min for the next periodic `clearTextureAtlas()`. Verify the periodic flush is firing via `[RENDER] periodic atlas flush` lines in `main.log`. If recurrence happens INSIDE the 10-min window, atlas saturation is faster than the timer — escalate to Phase 2 Canvas renderer.
- **S24→S25 lesson on hypothesis ranking under unreproducible bugs.** When the user can't reproduce a bug, ranking hypotheses by "what's defensively easy to fix" rather than "what evidence supports" can chase the wrong root cause for an entire round. S24 led with theme-swap and context-loss because they were code-tractable; the actual cause was gradual atlas saturation, which the existing evidence (one-off, no crash, no logged context loss) was already pointing toward. Rule going forward: in unreproducible-bug territory, rank hypotheses by what *evidence supports*, not by *what's easy to mitigate*. Defensive fixes for the wrong cause buy nothing but session burn — and worse, they create the false belief that the bug is fixed.

## Session Log — S27 (S33 trim 2026-07-07)

**2026-05-07 (session 27)** — comprehensive defensive pass + late-session iTerm reproduction reorients diagnosis (shipped in v1.4.0, validated S28)
- **Diagnosis**: user reported the garbled-glyph bug recurring multiple times/session for ~a week, triggered by Cmd+Tab away-and-back — on the S25 binary (asar 2026-04-27), disconfirming S25's saturation-only theory. New theory: macOS/Chromium silently invalidates WebGL texture state when Patton backgrounds; xterm's `onContextLoss` doesn't fire for this case.
- **Defences shipped** (commit `3cd768c`, ~120 LOC, 8 files): `backgroundThrottling: false` (window-manager.ts) to keep the renderer alive in background; `visibilitychange`+`window.focus` handlers → `flushAtlasOnResume()`→`clearTextureAtlas()` (1s-throttled, both events because macOS fires only one or the other); auto-reload (dispose+`loadWebgl`) if a flush throws; periodic interval 10→2 min; a renderer-log→`main.log` IPC bridge (`IPC.LOG_RENDERER` + `rlog()` in `renderer-logger.ts`, prefixed `[RENDERER]`) closing the S25 observability gap; `document.hidden`→`document.hasFocus()` in `app.ts` (backgroundThrottling pins visibilityState to 'visible'). ⌘⇧K = Reset Renderer is the manual escape hatch (shipped S25).
- **Late-session reorientation**: user produced a screenshot showing the SAME corruption signature in **iTerm2** (native Metal, no xterm/Electron) → bug likely a system-level macOS issue (CoreText font cache / Metal driver), not Patton-specific. S27's defences ship as defensive value regardless. Discriminator if it recurs: reproduce in Apple Terminal.app (CoreGraphics, no Metal). Validated in S28: zero recurrence in ~2 weeks.

## Decisions — S29 (S33 trim 2026-07-07)

- **2026-06-05 (S29) — Fixed glyph corruption with a renderer *choice*, not more atlas-flushing.** S27's flushes were firing 60k+× and still not preventing the recurrence → the WebGL atlas is the wrong layer to defend. xterm's DOM renderer has no GPU atlas, so it's structurally immune. Shipped as a user toggle (WebGL default, DOM opt-in) rather than auto-fallback: keeps WebGL's perf for the majority while giving a one-keystroke escape hatch. This is the long-shelved "Phase 2," now warranted because the contingency (recurrence inside the flush window) actually fired. Considered more flush heuristics — rejected as a same-layer band-aid.
- **2026-06-05 (S29) — `fast-uri` pinned via `overrides`, not `npm audit fix --force`.** The build gate failed on one prod high vuln (`fast-uri ≤3.1.1` via electron-store→conf→ajv). A surgical `overrides: {"fast-uri": "^3.1.2"}` patches it tree-wide without bumping any parent's major; `--force` would have churned the whole dev toolchain (electron-forge) with breaking majors. The 42 dev-only vulns (webpack/inquirer/tmp) don't ship and don't block the `--omit=dev` gate → left for a deliberate dep pass, not bundled into a render fix.
- **2026-06-05 (S29) — `macOptionClickForcesSelection: true` is a real gap fix, not cosmetic.** On macOS, xterm v6 ignores Shift for force-selection — only Option works, and only with this option set. Without it Patton had *no* way to drag-select while any app held the mouse (vim/tmux/htop, not just Claude Code fullscreen). Low-risk one-liner, matches iTerm2.
- **2026-06-05 (S29) — Shipped to main as one release after live user validation.** User confirmed "seems to be working" on the DOM renderer before the push. Pushing to main auto-releases; did it deliberately post-validation rather than gating behind more soak time, because the change is additive (WebGL still default) and reversible (toggle back). CI bumped MINOR (v1.5.0) because lines-since-v1.4.0-tag included S28's doc-heavy checkpoint, not because the code diff was large (133 LOC).

## Gotchas — iTerm2 system-level discriminator (S33 trim 2026-07-07)

- **Garbled-glyph "atlas eviction" symptom is not always a Patton bug** (S27 reorientation, 2026-05-07). User confirmed the same character-corruption signature reproduces in **iTerm2** during the period S25/S26/S27 spent diagnosing it as a Patton/xterm.js issue. Both Patton (WebGL via ANGLE/Metal) and iTerm2 (native Metal) sit downstream of the macOS GPU + CoreText font subsystem — a system-level issue (font-cache corruption, Metal driver state, OS update) presents identically in both. Discriminator: reproduce in **Apple Terminal.app** (CoreGraphics, no Metal). Same → font/CoreText layer; only Metal-using apps → GPU layer. Triage: `sudo atsutil databases -remove` + restart resets CoreText's glyph cache. Lesson: when a user can't reproduce on demand, check whether the bug reproduces in a different terminal app before any hypothesis about Patton internals.

## Session Log — 2026-05-31 (S28) (S34 trim 2026-07-07)

**2026-05-31 (session 28)** — shipped v1.4.0; S27 render fix validated, render saga closed
- **Render saga closed.** User confirmed zero garbled-glyph recurrence since ~2026-05-18. Production `main.log` corroborates S27 holding: `resume atlas flush` ×20,292 (visibility/focus handler firing on refocus), `periodic atlas flush` ×42,533 (2-min timer alive), `WebGL context lost` 10 / `re-acquired` 10 (all self-healed), `resume flush failed` 0, `snapshot saved` 0 (user never hit ⌘⇧K → no painful recurrence), `[RENDERER]` bridge ×62,845 (the S27 renderer-log IPC bridge works → S25 observability gap closed). Phase 2 (Canvas renderer) dropped — its trigger (recurrence inside the flush window) never fired.
- **Pushed 5 commits to main** (`f27ff09..d5d2417`: S25+S27 fixes + 3 checkpoints). CI computed 730 lines changed since v1.3.0 → MINOR → **v1.4.0** (one release covering both fixes; HEAD commit lacked `[no-release]` so the workflow ran once). Release published with `Patton-1.4.0-arm64.dmg` + SHA-256.
- **Token question resolved.** `release.yml` uses two tokens: `${{ github.token }}` (ephemeral, GitHub-release step — never the user's concern) and `secrets.HOMEBREW_PAT` (user-managed, cask step, set 2026-03-09). Local push uses the gh keyring `gho_` OAuth token (no expiry). The "expiring token" GitHub emailed about is almost certainly HOMEBREW_PAT — confirmed expired: the v1.4.0 run's `Update Homebrew cask` was the only failed step; the release itself succeeded. User deprioritized brew, so left as-is.
- **Local install is code-current but version-cosmetically stale.** The installed asar (May 7 build) contains all S27 fix literals → byte-equivalent code to v1.4.0 (the only v1.4.0 commit post-dating it is doc-only `d5d2417`); but it self-reports 1.0.0 (npm-run-package quirk, confirmed via crashpad `_version=1.0.0`). No relaunch needed for fixes; reinstall the DMG only for a correct version string.
- **No code changes this session** — diagnose + ship + validate only. Standing user action: revoke the burned `github_pat_` pasted in another transcript.
